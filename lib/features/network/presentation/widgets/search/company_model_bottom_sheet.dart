import 'package:flutter/material.dart';

class SimplifiedWidget extends StatelessWidget {
  const SimplifiedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
