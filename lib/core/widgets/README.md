# Internet Connectivity Widgets

هذا الملف يحتوي على شرح لكيفية استخدام الـ Internet Connectivity Features في التطبيق.

## المكونات الرئيسية

### 1. ConnectivityHelper
خدمة للتحقق من اتصال الإنترنت باستخدام `connectivity_plus`.

**الميزات:**
- ✅ Stream يعمل بشكل صحيح لمراقبة التغييرات في الاتصال
- ✅ دالة للتحقق من الاتصال الحالي
- ✅ متوافق مع Dependency Injection

**الموقع:**
```
lib/core/services/network/internet_checker/connectivity_helper.dart
```

**الاستخدام:**
```dart
// في الـ Cubit أو الـ Controller
final connectivityHelper = sl<ConnectivityHelper>();

// التحقق من الاتصال الحالي
final isConnected = await connectivityHelper.checkConnectivity();

// الاستماع للتغييرات
connectivityHelper.getConnectivityStream().listen((isConnected) {
  if (isConnected) {
    // متصل بالإنترنت
  } else {
    // غير متصل
  }
});
```

---

### 2. NoInternetScreen
شاشة فخمة ومودرن تظهر عند انقطاع الإنترنت.

**الميزات:**
- 🎨 تصميم عصري وجذاب
- ✨ أنيميشن احترافي
- 🌗 دعم Dark Mode و Light Mode
- 📱 Responsive Design
- 🔄 زر إعادة المحاولة
- 💡 نصائح للمساعدة

**الموقع:**
```
lib/core/widgets/no_internet_screen.dart
```

**الاستخدام المباشر:**
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => NoInternetScreen(
      onRetry: () {
        // كود إعادة المحاولة
        Navigator.pop(context);
      },
    ),
  ),
);
```

---

### 3. InternetWrapper
Widget يقوم بـ:
- ✅ التحقق التلقائي من الإنترنت
- ✅ عرض NoInternetScreen عند الانقطاع
- ✅ الاستماع المستمر للتغييرات
- ✅ إشعارات SnackBar عند التغيير
- ✅ التحقق عند عودة التطبيق للمقدمة

**الموقع:**
```
lib/core/widgets/internet_wrapper.dart
```

**التطبيق:**
تم دمج الـ InternetWrapper في `app.dart` بشكل تلقائي، لذلك جميع شاشات التطبيق محمية تلقائياً.

```dart
// في app.dart
builder: (context, child) {
  return SafeArea(
    child: InternetWrapper(
      child: child!,
    ),
  );
},
```

---

## كيفية العمل

1. **عند بدء التطبيق:**
   - يتحقق الـ InternetWrapper من الاتصال الحالي
   - إذا كان متصل: يعرض التطبيق بشكل طبيعي
   - إذا كان غير متصل: يعرض NoInternetScreen

2. **أثناء استخدام التطبيق:**
   - يستمع الـ InternetWrapper للتغييرات في الاتصال
   - عند الانقطاع: يعرض NoInternetScreen تلقائياً
   - عند العودة: يعود للشاشة الأصلية ويظهر SnackBar أخضر

3. **عند الضغط على "إعادة المحاولة":**
   - يتحقق من الاتصال مرة أخرى
   - إذا عاد الاتصال: يعود للتطبيق

---

## الاختبار

لاختبار الميزة:

1. **على المحاكي:**
   - افصل الإنترنت من الجهاز
   - شغّل التطبيق أو افتحه
   - ستظهر شاشة No Internet

2. **على الجهاز الحقيقي:**
   - فعّل وضع الطيران
   - أو افصل الواي فاي والبيانات
   - التطبيق سيكتشف الانقطاع تلقائياً

---

## التخصيص

### تخصيص NoInternetScreen

يمكنك تعديل:
- الألوان في ملف `no_internet_screen.dart`
- النصوص والترجمات
- النصائح المعروضة
- مدة الأنيميشن

### تخصيص InternetWrapper

يمكنك تعديل:
- SnackBar المعروض
- سلوك إعادة المحاولة
- مدة انتظار التحقق

---

## الملاحظات المهمة

⚠️ **تأكد من:**
- تشغيل `flutter pub run build_runner build` بعد أي تعديلات
- الـ Connectivity متسجل في `external.dart`
- الـ ConnectivityHelper متوفر في الـ BlocProvider

✅ **تم التنفيذ:**
- إصلاح ConnectivityHelper ليعمل Stream بشكل صحيح
- دمج InternetWrapper في التطبيق
- تحديث StartCubit لاستخدام ConnectivityHelper بشكل أفضل

---

## Dependencies المستخدمة

```yaml
dependencies:
  connectivity_plus: ^7.0.0
  flutter_animate: ^4.5.2
  animate_do: ^4.2.0
  flutter_screenutil: ^5.9.3
```

---

تم التطوير بواسطة: AI Assistant 🤖
التاريخ: 2026-01-13
